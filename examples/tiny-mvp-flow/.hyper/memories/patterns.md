# Patterns

- GOAL-0001 reusable patterns: Validate the primary browser flow with one smoke pass after every UI runtime packet.
- GOAL-0001 reusable patterns: Keep runtime packet evidence specific enough that the next packet can continue without rereading unrelated notes.
- GOAL-0001 learn pattern: Use one browser smoke pass for create, complete, delete, reload on each task-list UI packet.
- GOAL-0001 validated: `npm run build` passed.
